const onSearch = async (localSearch?: any) => {
  //call the local search function
  //format the local search response based on ONDC template
  // Sign the respones body using ONDC specification
  // Send to the gateway URL , i.e. axios is needed here.
};

export default onSearch;
